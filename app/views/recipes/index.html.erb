<%= content_for :title, "Recipes" %>

<main class='main'>
  <div class='header'>
    <h1>Recipes by <%= current_user.name %></h1>
    <div class='actions'>
     <form action="<%= new_recipe_path %>" method="get">
  <input type="submit" value="Add New Recipe" class="custom-button">
</form>
  </div>

  <ul class='recipes'>
    <% if @recipes.empty? %>
      <li class='no-recipes'>You have no recipes yet!</li>
    <% else %>
      <% @recipes.each do |recipe| %>
        <li class='recipess'>
          <div class='left'>
            <h2><%= link_to recipe.name, recipe %></h2>
            <div class='actions'>
              <%= button_to 'REMOVE', recipe_path(recipe), method: :delete %>
            </div>
          </div>

          <div class='right'>
            <p class='description'><%= recipe.description %></p>
          </div>
        </li>
      <% end %>
    <% end %>
  </ul>
</main